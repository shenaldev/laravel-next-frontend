const authUrls = {
  login: "/login",
  register: "/register",
  logout: "/logout",
  sendVerificationMail: "/email-verification",
  verifyCode: "/email-verify",
};

const apiUrls = {
  user: "/user",
};

export { apiUrls, authUrls };
